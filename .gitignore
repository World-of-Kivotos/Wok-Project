# Gradle / 构建产物
build/
.gradle/
out/
bin/

# Forge 运行目录与生成数据
run/
runs/
src/generated/

# IDE
.idea/
*.iml
*.ipr
*.iws
.eclipse/
.settings/
.classpath
.project
*.launch
.vscode/

# Claude Code 本地工具配置 (launch.json 等, 每人本地, 不入库)
.claude/

# 前端 (webui/ monorepo, 见 WebUI_Architecture_DesignSpec 10.2)
webui/node_modules/
webui/dist/
# 依赖预打包缓存。默认落在 webui/node_modules/.vite, 但从别处调起 vite 时会在 webui/ 根另生成一份。
webui/.vite/

# AI 助手提示词 (各人本地一份, 不入库; 人类可读的协作规范见 分支协作.md)
CLAUDE.md
CODEX.md
AGENTS.md
GEMINI.md
.cursorrules
.github/copilot-instructions.md

# 系统
*.log
.DS_Store

# 裸 javac / 工具临时产物 (非交付物, 勿入库)
/net/
/javac.*.args
/.VSCodeCounter/
# 根目录散落的外部 mod jar (本地调试用, 非源码)
/*-forge-*.jar
/*.jar
# 外部 mod 依赖 jar (TACZ/Champions, 本地 deobf 编译依赖; 体积大且属第三方, 不入库)
/libs/
