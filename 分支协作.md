# 分支协作规范

本仓库 `World-of-Kivotos/Wok-Project`(Minecraft Forge 1.20.1 mod, modid `miningdim`)的分支模型、提交规范与合并前硬门。新人入手先读本文 + 各子系统真源 spec(见 `docs/`)。

---

## 一、分支模型

| 分支 | 角色 | 规则 |
|---|---|---|
| `main` | 稳定线(受保护) | 只接【已编译通过 + GameTest 全绿】的合并;不直接在 main 上写功能 |
| `feat/<名>` | 功能开发 | 从 main 切出;一个分支一条功能闭环 |
| `fix/<名>` | 缺陷修复 | 从 main 切出 |
| `docs/<名>` | 文档/设计 spec | 纯文档可不跑 GameTest |
| `chore/<名>` | 构建/杂务 | 依赖升级、配置、清理 |

命名用小写 + 短横线,语义化:`feat/market-admin-curate`、`fix/server-crash-capability`。

当前活跃分支(随时更新):
- `main` — 稳定线
- `feat/market-admin-curate` — 跳蚤市场 V0 基准价 admin curate 后台(base_values 表 + OP 动作 + React 面板)
- `feat/coherent-noise-carving` — 矿业维度 worldgen 连贯噪声雕刻
- `docs/job-and-economy-specs` — 职业与经济设计 spec

---

## 二、标准工作流

```
git checkout main && git pull            # 同步最新稳定线
git checkout -b feat/<名>                # 从 main 切功能分支
#  ... 开发 ...
#  ... 编译 + GameTest 必须全绿(见第四节)...
git add <精确文件>                        # 只暂存本任务文件, 不夹带无关改动
git commit                               # 原子提交, 中文 Conventional Commits(见第三节)
git checkout main
git merge --ff-only feat/<名>            # 能 ff 就 ff(线性历史); 不能 ff 先 rebase 到 main
git push origin main
```

合并前若 main 已前进:先 `git rebase main` 把功能分支接到最新 main 上,重跑 GameTest,再合。保持 main 线性、可回溯。

---

## 三、提交规范(硬性)

- **Conventional Commits + 中文**:`feat(market): 跳蚤市场偏离手续费 v1`、`fix(worldgen): 修复服务端加载崩溃`。type 用 `feat/fix/docs/chore/refactor/test`,scope 用子系统名(market/champion/worldgen/economy/job/webui...)。
- **原子提交**:每个功能/修复闭环立即提交;一次提交只做一件事。
- **精准暂存**:`git add` 只加本任务文件,绝不顺手带无关改动(格式、导入顺序、别的 WIP)。
- **零 emoji**:代码、注释、commit message、文档、PR 一律禁 emoji;严重度/状态用纯文本(Critical/Major/Minor、PASS/FAIL)或 ASCII 记号。
- **身份纯洁**:commit 只留个人签名,**严禁任何 AI 署名 / Co-Authored-By**。
- **禁空壳**:不留 `TODO: implement` / 空方法逃课;缺信息先指出缺什么,不静默留空。

---

## 四、合并前硬门:编译 + GameTest 全绿

写入不等于成功。合回 main 前**必须**本地验证通过。

构建必须用项目自带的 **JDK17**(本机默认 Java21 + Gradle9 与 Forge 不兼容):

```bash
# JDK17 随 gradle 拉到 ~/.gradle/jdks/eclipse_adoptium-17-amd64-windows/jdk-17.0.18+8
export JAVA_HOME="C:/Users/<你>/.gradle/jdks/eclipse_adoptium-17-amd64-windows/jdk-17.0.18+8"
# 或 PowerShell: $env:JAVA_HOME = "...\jdk-17.0.18+8"

./gradlew compileJava        # 先编译抓语法/类型错(~1 分钟)
./gradlew runGameTestServer  # 跑全套 GameTest(服务端进程, 几分钟)
```

**GameTest 结果以日志为准, 不信 gradle 退出码**(退 0 是假阳性):看 `run/logs/latest.log` 末尾的
`All N required tests passed :)`。出现任何 `Failed to pass` 即不得合并。

构建坑(已知):
- JDK 必须 17,见上。
- SQLite/JDBC 在 dev 运行期需 `minecraftLibrary` 配置 + 显式 `Class.forName("org.sqlite.JDBC")`(已配,改 store 层时勿动)。
- 客户端类(MCEF/Screen)关进 `DistExecutor` 必须双箭头 `() -> () -> Client.method()` + `unsafeRunWhenOn`,否则服务端 CONSTRUCT 崩。

---

## 五、不入库的东西(见 `.gitignore`)

`build/` `.gradle/` `run/` `runs/` `src/generated/`、IDE 配置(`.idea/`/`.vscode/`)、`.claude/`(Claude Code 本地工具配置)、`/libs/`(TACZ/Champions 第三方 deobf 编译依赖 jar,本地放,体积大不入库)、散落根目录的 `*.jar`、`*.log`。

外部 mod 依赖(TACZ/Champions)走 `compileOnly` 本地 jar,新人需自备 `libs/` 下对应 deobf jar 才能编译。

---

## 六、真源与设计文档

改任何子系统前先读其设计 spec(`docs/` 下),数值/契约以 spec 为准,不凭记忆改写。例:
- 经济反洗钱评审:`docs/Economy_Laundering_Review.md`
- WebUI 架构:`docs/WebUI_Architecture_DesignSpec.md`
- 精英怪星级词条:`ChampionStarAffix` spec(数值落地见 `champion/AffixDef.java` 35 词条表)

代码即真源的地方(枚举/常量表)改动须回 spec 对照,改一处数值连带的钳制/红线一起核。
